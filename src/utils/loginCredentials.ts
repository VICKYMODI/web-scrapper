import * as Bcrypt from 'bcrypt';
export class Utils{

    
    static generateVarificationCredential(){
       // size:number = 5
        const amwins = {
            userId : "",
            password : ""
        }

         const connect = {
            userId : "",
            password : ""
        }
        return 
        // return parseInt(otp)
    }

}